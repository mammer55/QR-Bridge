{
  "name": "qr-bridge",
  "version": "2.0.0",
  "description": "Clipboard bridge via Supabase",
  "private": true,
  "scripts": {
    "dev": "npx live-server --port=3000 --open=index.html --no-browser=false .",
    "start": "npx live-server --port=3000 ."
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
